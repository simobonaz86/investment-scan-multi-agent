services:
  api:
    build: .
    container_name: invest-scan-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - invest_scan_data:/app/data
    restart: unless-stopped

volumes:
  invest_scan_data:

